---
interface Props {
  fluid?: boolean;
  class?: string;
}

const {
  fluid = false,
  class: className,
} = Astro.props;
---

<div
  class:list={[
    !fluid && "container",
    fluid && "w-full",
    className,
  ]}
>
  <slot />
</div>